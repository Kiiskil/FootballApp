@page "/fetchdata"

@using FootballApp.Data
@inject WeatherForecastService ForecastService
@inject FootballService  FootballService

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (forecasts == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <!--<table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var forecast in forecasts)
            {
                <tr>
                    <td>@forecast.Date.ToShortDateString()</td>
                    <td>@forecast.TemperatureC</td>
                    <td>@forecast.TemperatureF</td>
                    <td>@forecast.Summary</td>
                </tr>
            }
        </tbody>
    </table>-->
    <table class="table">
        <tbody>
            @foreach (var match in _matches.Matches)
            {
                <tr>
                    <td>@match.Id</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    WeatherForecast[] forecasts;
    Matchlist _matches = new Matchlist();
    string url = "https://functionapp2018071101324.blob.core.windows.net/data/matches_latest.json";

    protected override async Task OnInitializedAsync()
    {
        
        forecasts = await ForecastService.GetForecastAsync(DateTime.Now);
        //Need to be async method?
        _matches.Matches = FootballService._download_serialized_json_data<List<Match>>(url); 
    }
}
